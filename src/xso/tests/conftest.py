"""Shared pytest fixtures for the XSO test suite.

Currently empty — add fixtures here when multiple test modules need to
share setup code (e.g., building common minimal models, stubbing out
solver backends for unit tests).
"""